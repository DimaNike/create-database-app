{
    "name": "mleapp",
    "version": "1.0.0",
    "devDependencies": {
        "mle-js": "^23.7.0",
        "typescript": "^5.7.3",
        "esbuild": "0.25.1",
        "oracledb": "^6.7.1",
        "vitest": "^3.1.2",
        "dotenv": "^16.5.0",
        "mle-cli": "0.1.3"
    },
    "scripts": {
        "ords": "mle-cli --debug --experimental build -c 'userc/userc@localhost:1235/FREEPDB1' create ords-fs",
        "test": "vitest"
    }
}